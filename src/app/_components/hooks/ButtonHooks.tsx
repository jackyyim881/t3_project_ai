import React from "react";

const ButtonHooks: React.FC = () => {
  const handleClick = () => {
    // Handle button click event here
  };

  return <button onClick={handleClick}>Click me</button>;
};

export default ButtonHooks;
